rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Abhi ke liye sab band (secure)
      allow read, write: if false;
    }
  }
}
