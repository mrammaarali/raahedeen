{
  "name": "raahedeen-functions",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": "18" },
  "scripts": {
    "build": "tsc -p .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
